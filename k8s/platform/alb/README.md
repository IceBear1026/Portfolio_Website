# Replaced: Azure Application Gateway for Containers ALB resources

This folder used to contain the active ApplicationLoadBalancer resource.

The old working AGC version is preserved in:

../alb-agc-archived/

The active cheaper path should not apply Application Gateway for Containers resources.
